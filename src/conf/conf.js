const conf = {
    emailjsservicekey : String(import.meta.env.VITE_SERVICE_ID),
    emailjstemplatekey : String(import.meta.env.VITE_TEMPLATE_ID),
    emailjspublickey : String(import.meta.env.VITE_PUBLIC_ID),
}

export default conf;